# Patient Lookup System - نظام البحث عن المريض

نظام متكامل للبحث عن بيانات المرضى باستخدام رمز QR أو إدخال الكود يدوياً.

## 🌟 المميزات

- **مسح رمز QR**: استخدم كاميرا الجهاز لمسح رمز QR الخاص بالمريض
- **البحث اليدوي**: أدخل كود المريض يدوياً للبحث
- **واجهة عربية**: تصميم كامل باللغة العربية مع دعم RTL
- **تصميم متجاوب**: يعمل على جميع الأجهزة (موبايل، تابلت، كمبيوتر)
- **عرض شامل للبيانات**:
  - المعلومات الأساسية للمريض
  - الحالات الطبية
  - الفواتير وملخص المدفوعات
  - المواعيد
  - الصور الطبية

## 📁 الملفات

```
clinic-Website/
├── patient-lookup.html          # صفحة البحث عن المريض الرئيسية
├── qr-generator-demo.html       # صفحة تجريبية لتوليد QR codes
└── assets/
    └── js/
        └── patient-lookup.js    # JavaScript للتعامل مع API و QR
```

## 🚀 كيفية الاستخدام

### 1. إعداد الملفات

تأكد من وجود جميع الملفات في المكان الصحيح:

- `patient-lookup.html` - الصفحة الرئيسية
- `assets/js/patient-lookup.js` - ملف JavaScript
- `qr-generator-demo.html` - صفحة التجريب (اختيارية)

### 2. فتح الصفحة

افتح `patient-lookup.html` في المتصفح أو على خادم ويب.

**ملاحظة مهمة**: لاستخدام ماسح QR، يجب فتح الصفحة عبر:

- `https://` (SSL)
- أو `http://localhost`
- متصفحات الويب الحديثة تتطلب HTTPS لاستخدام الكاميرا

### 3. البحث عن مريض

#### الطريقة الأولى: البحث اليدوي

1. أدخل كود المريض في حقل البحث
2. اضغط على زر "بحث" أو Enter

#### الطريقة الثانية: مسح QR

1. اضغط على زر "مسح رمز QR"
2. امنح الإذن للمتصفح لاستخدام الكاميرا
3. وجّه الكاميرا نحو رمز QR
4. سيتم البحث تلقائياً بعد المسح

#### الطريقة الثالثة: رابط مباشر

يمكنك إنشاء رابط مباشر مع الكود:

```
https://your-domain.com/patient-lookup.html?code=PATIENT_CODE
```

## 🔧 التكامل مع النظام الآخر

عند طباعة وصفة طبية (RX) في النظام الآخر، قم بتوليد QR code يحتوي على:

```
https://your-domain.com/patient-lookup.html?code=PATIENT_CODE
```

حيث `PATIENT_CODE` هو الكود الفريد للمريض.

### مثال على توليد QR في PHP:

```php
<?php
$patientCode = "PAT12345";
$websiteUrl = "https://your-domain.com/patient-lookup.html";
$qrUrl = $websiteUrl . "?code=" . urlencode($patientCode);

// استخدم مكتبة QR code لتوليد الصورة
// مثال: استخدام chillerlan/php-qrcode
$qrcode = (new QRCode)->render($qrUrl);
?>
```

## 🧪 التجربة والاختبار

استخدم `qr-generator-demo.html` لتجربة النظام:

1. افتح `qr-generator-demo.html` في المتصفح
2. أدخل كود مريض تجريبي
3. احدّث رابط الموقع إذا لزم الأمر
4. اضغط على "توليد رمز QR"
5. امسح الكود المولّد أو اضغط على الرابط

## 🔌 API Integration

النظام يتصل بـ API التالي:

```
POST https://smartclinicv5.tctate.com/api/public/patient/lookup
```

**Body Parameters:**

```json
{
  "code": "PATIENT_CODE"
}
```

**Response Example:**

```json
{
  "success": true,
  "patient": {
    "id": 5331,
    "name": "مرتضى مصطفى علي",
    "age": 18,
    "phone": "0782 785 2083",
    "email": null,
    "sex": 0,
    "birth_date": "2008-01-01",
    "address": null,
    "systemic_conditions": null,
    "created_at": "2026-01-01T14:26:32.000000Z"
  },
  "cases": [...],
  "bills": [...],
  "reservations": [...],
  "images": [...]
}
```

## 🎨 التخصيص

### تغيير الألوان

في `patient-lookup.html`، قم بتعديل متغيرات CSS:

```css
:root {
  --primary-color: #00d4ff;
  --secondary-color: #0066ff;
  --success-color: #28a745;
  --danger-color: #dc3545;
  --warning-color: #ffc107;
}
```

### تغيير API URL

في `assets/js/patient-lookup.js`، قم بتعديل:

```javascript
const API_URL = "https://your-api-url.com/api/public/patient/lookup";
```

## 📱 متطلبات المتصفح

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

**لاستخدام QR Scanner:**

- يجب أن يكون الموقع على HTTPS أو localhost
- يجب منح إذن الكاميرا

## 🐛 حل المشاكل

### الكاميرا لا تعمل

- تأكد من أن الموقع يعمل على HTTPS أو localhost
- تحقق من أذونات المتصفح للكاميرا
- جرب متصفح آخر

### لا تظهر البيانات

- تحقق من أن API يعمل
- افتح Developer Console (F12) للتحقق من الأخطاء
- تأكد من أن الكود صحيح

### مشاكل في العرض

- تأكد من وجود جميع ملفات CSS
- امسح الـ cache وأعد تحميل الصفحة
- تحقق من أن المتصفح محدث

## 📄 الترخيص

هذا المشروع مجاني للاستخدام الشخصي والتجاري.

## 👨‍💻 الدعم

لأي استفسارات أو مشاكل، يرجى التواصل مع فريق التطوير.

---

**تم التطوير بواسطة GitHub Copilot** 🤖
